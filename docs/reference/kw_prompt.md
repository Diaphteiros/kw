## kw prompt

Generate a script that generates a prompt to display in the shell

### Synopsis

Generate a script that generates a prompt to display in the shell.

The script can be called manually or added to your shell's profile.

### Options

```
  -h, --help   help for prompt
```

### Options inherited from parent commands

```
      --debug   Print debug information to stderr.
```

### SEE ALSO

* [kw](kw.md)	 - Quickly switch between multiple Kubernetes clusters
* [kw prompt bash](kw_prompt_bash.md)	 - Generate a bash script that generates a prompt to display in the shell
* [kw prompt zsh](kw_prompt_zsh.md)	 - Generate a zsh script that generates a prompt to display in the shell

