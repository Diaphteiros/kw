## kw prompt bash

Generate a bash script that generates a prompt to display in the shell

### Synopsis

Generate a bash script that generates a prompt to display in the shell.

The script can be called manually or added to your shell's profile.

```
kw prompt bash [flags]
```

### Options

```
  -h, --help   help for bash
```

### Options inherited from parent commands

```
      --debug   Print debug information to stderr.
```

### SEE ALSO

* [kw prompt](kw_prompt.md)	 - Generate a script that generates a prompt to display in the shell

